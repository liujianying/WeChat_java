package com.tencent.mm.ui.tools;

import android.view.View;
import android.view.View.OnClickListener;

class HardCoderSettingUI$4 implements OnClickListener {
    final /* synthetic */ HardCoderSettingUI uzO;

    HardCoderSettingUI$4(HardCoderSettingUI hardCoderSettingUI) {
        this.uzO = hardCoderSettingUI;
    }

    public final void onClick(View view) {
        boolean isChecked = HardCoderSettingUI.a(this.uzO).isChecked();
        HardCoderSettingUI.b(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.c(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.d(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.e(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.f(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.g(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.h(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.i(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.j(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.k(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.l(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.m(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.n(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.o(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.p(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.q(this.uzO).setChecked(isChecked);
        HardCoderSettingUI.r(this.uzO).setChecked(isChecked);
    }
}
