package com.tencent.tencentmap.mapsdk.a;

import java.util.ArrayList;

public class pk implements pi {
    private ArrayList<pi> a;

    public synchronized boolean a(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).a(f, f2);
        }
        return false;
    }

    public synchronized boolean b(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).b(f, f2);
        }
        return false;
    }

    public synchronized boolean c(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).c(f, f2);
        }
        return false;
    }

    public synchronized boolean d(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).d(f, f2);
        }
        return false;
    }

    public synchronized boolean e(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).e(f, f2);
        }
        return false;
    }

    public synchronized boolean f(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).f(f, f2);
        }
        return false;
    }

    public synchronized boolean g(float f, float f2) {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).g(f, f2);
        }
        return false;
    }

    public synchronized void a() {
        for (int size = this.a.size() - 1; size >= 0; size--) {
            ((pi) this.a.get(size)).a();
        }
    }
}
